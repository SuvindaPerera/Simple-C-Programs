#include <stdio.h>

int main()
{
    // Declare a variable to store the user input
    int a;

    // Tell the user what to do
    printf("Enter a number: ");

    // Get the user input
    scanf("%d", &a);

    // Print the user input
    printf("What you have enterd is %d", a);

    return 0;
}